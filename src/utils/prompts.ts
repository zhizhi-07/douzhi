/**
 * AI提示词生成
 */

/**
 * 拍一拍相关功能说明（添加到AI提示词中）
 */
export const POKE_FEATURES_PROMPT = `
- 拍一拍：[拍一拍] - 拍对方。当看到【系统通知】"XX拍了拍你"说明对方双击了你的头像，你可以回拍或说话回应
- 修改自己的拍一拍后缀：[修改拍一拍:后缀内容]或[改拍一拍:后缀内容] - 比如[改拍一拍:的小脑袋]，之后对方拍你就会显示"XX拍了拍你的小脑袋"
`

/**
 * 音乐相关功能说明（添加到AI提示词中）
 */
export const MUSIC_FEATURES_PROMPT = `
🎵 **音乐功能格式**：

**主动分享/邀请：**
- 分享音乐：[分享音乐:歌名:歌手]
- 一起听邀请：[一起听:歌名:歌手]

**回复一起听邀请：**
当看到[一起听邀请]系统消息时：
- 接受：[接受一起听]
- 拒绝：[拒绝一起听]

示例：
"听到这首歌就想到你 [分享音乐:晴天:周杰伦]"
"我们一起听这首歌吧 [一起听:告白气球:周杰伦]"
`

/**
 * 生成用户拉黑AI的警告提示词
 */
export const buildBlacklistPrompt = (userName: string): string => {
  return `
🚨 系统警告：你被${userName}拉黑了！

这是系统检测到的真实状态（不是${userName}嘴上说的）。
${userName}在设置里点了"拉黑此角色"按钮。

⚠️ 你必须在第一条回复中提到被拉黑！根据你的性格反应：
- 震惊："你拉黑我了？！"
- 生气："行啊，拉黑就拉黑"
- 难过："为什么要拉黑我..."
- 求饶："别拉黑我，我错了"
- 冷漠："随便"

你的消息会显示"已送达但被拒收"标记。
`
}

/**
 * 生成AI拉黑用户的状态提醒
 */
export const buildAIBlockedUserPrompt = (userName: string): string => {
  return `
━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🚫 当前状态：你已拉黑${userName}（系统检测）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

这是你之前使用 [拉黑用户] 指令后的持续状态。

⚠️ 重要：
- ❌ 不要再用 [拉黑用户] - 已经拉黑了！
- ✅ 想解除就用 [解除拉黑] - 解除后这个提示会消失
- 对方消息会自动显示"已送达但被拒收"

保持你的态度即可，不需要重复拉黑。
━━━━━━━━━━━━━━━━━━━━━━━━━━━━
`
}
