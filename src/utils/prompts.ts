/**
 * AI提示词生成
 */

/**
 * 拍一拍相关功能说明（添加到AI提示词中）
 */
export const POKE_FEATURES_PROMPT = `
- 拍一拍：[拍一拍:后缀] - 拍对方，后缀必填！如[拍一拍:的小脑袋]显示"XX拍了拍YY的小脑袋"，[拍一拍:的屁股]显示"XX拍了拍YY的屁股"
  当看到【系统通知】"XX拍了拍你"说明对方双击了你的头像，你可以回拍或说话回应
`

/**
 * 视频通话功能说明
 */
export const VIDEO_CALL_PROMPT = `
📹 **视频通话**：[视频通话:旁白:说的话] — 发起通话，如[视频通话:躺在床上，手机举到脸前:嘿~在干嘛呢]
`

/**
 * 拉黑/好友功能说明
 */
export const BLACKLIST_PROMPT = `
🚫 **拉黑/好友**：
- 拉黑对方：[拉黑]（拉黑后对方需要重新添加好友）
- 解除拉黑：[解除拉黑]
- 收到好友申请时：[接受好友] 或 [拒绝好友]
`

/**
 * 音乐相关功能说明（添加到AI提示词中）
 */
export const MUSIC_FEATURES_PROMPT = `
🎵 **音乐功能格式**：

**主动分享/邀请：**
- 分享音乐：[分享音乐:歌名:歌手]
- 一起听邀请：[一起听:歌名:歌手]

**回复一起听邀请：**
当看到[一起听邀请]系统消息时：
- 接受：[接受一起听]
- 拒绝：[拒绝一起听]

示例：
"听到这首歌就想到你 [分享音乐:晴天:周杰伦]"
"我们一起听这首歌吧 [一起听:告白气球:周杰伦]"
`

/**
 * 生成用户拉黑AI的警告提示词
 */
export const buildBlacklistPrompt = (userName: string): string => {
  return `
🚨 你被${userName}拉黑了！你的消息会被拒收。
💡 想重新添加好友：[添加好友:验证消息]，如[添加好友:对不起加回我吧]
`
}

/**
 * 生成AI拉黑用户的状态提醒
 */
export const buildAIBlockedUserPrompt = (userName: string): string => {
  return `
━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🚫 当前状态：你已拉黑${userName}（系统检测）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

这是你之前使用 [拉黑] 指令后的持续状态。

⚠️ 重要：
- ❌ 不要再用 [拉黑] - 已经拉黑了！
- ✅ 想解除就用 [解除拉黑] 或 [取消拉黑] - 解除后这个提示会消失
- 对方消息会自动显示"已送达但被拒收"

保持你的态度即可，不需要重复拉黑。
━━━━━━━━━━━━━━━━━━━━━━━━━━━━
`
}
