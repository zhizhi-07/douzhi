# 小剧场模板架构 v2.0

## 🎯 为什么重构？

旧架构：**所有43个模板挤在一个3000行文件里** → 后续1万个模板完全没法维护

新架构：**每个模板独立文件 + 按分类组织** → 可扩展到无限模板

## 📁 目录结构

```
templates/
├── index.ts                 # 自动导入入口
├── README.md               # 本文档
│
├── social/                 # 社交通讯
│   ├── groupChat.ts       # 群聊记录
│   ├── privateChat.ts     # 私聊记录
│   ├── xiaohongshu.ts     # 小红书 ✅ 已迁移
│   └── ...
│
├── life/                   # 生活消费
│   ├── receipt.ts         # 小票
│   ├── menu.ts            # 菜单
│   └── ...
│
├── work/                   # 工作学习
│   ├── diary.ts           # 日记
│   ├── memo.ts            # 备忘录
│   └── ...
│
├── emotion/                # 情感关系
├── entertainment/          # 娱乐休闲
├── health/                 # 健康医疗
├── document/               # 证件文书
├── transport/              # 交通出行
├── privacy/                # 隐私安全
└── tool/                   # 工具应用
```

## ✅ 新模板规范

每个模板文件格式：

```typescript
import { TheatreTemplate } from '../../theatreTemplates'

export const xxxTemplate: TheatreTemplate = {
  id: 'unique_id',
  name: '模板名称',
  category: '分类名称',
  keywords: ['关键词1', '关键词2'],
  fields: [
    { key: 'FIELD1', label: '字段1', placeholder: '示例' },
    // ...
  ],
  htmlTemplate: `
    <!-- HTML内容 -->
  `.trim()
}
```

## 🚀 添加新模板

1. **创建文件**：在对应分类目录创建 `xxx.ts`
2. **导出模板**：按照上面规范编写
3. **注册模板**：在 `index.ts` 中 import 并添加到数组

示例：
```typescript
// 1. 创建 templates/social/weibo.ts
export const weiboTemplate: TheatreTemplate = { ... }

// 2. 在 templates/index.ts 注册
import { weiboTemplate } from './social/weibo'
export const allTemplates = [..., weiboTemplate]
```

## 📦 迁移进度

- [x] 小红书分享 (xiaohongshu.ts)
- [ ] 其余42个模板待迁移

## 🔧 迁移脚本

使用以下脚本自动拆分现有模板：

```bash
# 运行迁移脚本（待创建）
npm run migrate-templates
```

## 💡 优势

1. **可维护性** - 每个模板独立，修改互不影响
2. **可扩展性** - 支持1万+模板，按需加载
3. **团队协作** - 多人可同时编辑不同模板
4. **分类清晰** - 文件夹结构一目了然
5. **版本控制** - Git更友好，冲突少
