# 头像框功能测试指南

## 🎯 功能说明
在聊天设置中添加了头像框自定义功能，可以为用户和AI的头像添加炫酷的边框效果。

## 📝 测试步骤

### 1. 进入聊天设置
1. 打开任意聊天
2. 点击右上角设置按钮
3. 滚动到"头像框"部分
4. 点击展开

### 2. 选择预设样式
预设样式列表：
- **默认**：无边框
- **金色光环**：金色渐变边框 + 发光效果
- **彩虹边框**：彩虹渐变 + 旋转动画
- **霓虹发光**：青色/粉色霓虹 + 脉冲动画
- **钻石闪耀**：白色渐变 + 闪烁动画
- **火焰边框**：红黄渐变 + 闪烁效果
- **冰霜边框**：蓝色边框 + 冰霜质感

点击任意预设，会自动填充到输入框并显示预览。

### 3. 预览效果
- 预览区域会显示用户和AI的头像框效果
- 可以实时看到边框样式

### 4. 应用设置
- 点击"应用"按钮
- 返回聊天页面
- 查看消息列表中的头像是否显示边框

### 5. 自定义CSS（高级）
可以在输入框中输入自定义CSS，例如：
```css
border: 3px solid #FF1493 !important;
box-shadow: 0 0 20px rgba(255, 20, 147, 0.8) !important;
border-radius: 50% !important;
```

## 🐛 故障排查

### 问题1：头像框不显示
**可能原因：**
- chatId未正确传递
- localStorage未保存
- 样式未注入

**解决方法：**
1. 打开浏览器控制台（F12）
2. 查看localStorage：
   ```javascript
   localStorage.getItem('user_avatar_frame_你的chatId')
   localStorage.getItem('ai_avatar_frame_你的chatId')
   ```
3. 检查是否有CSS内容
4. 刷新页面重试

### 问题2：预览正常但聊天页面不显示
**可能原因：**
- MessageItem未接收chatId
- Avatar组件未加载样式

**解决方法：**
1. 检查控制台是否有错误
2. 确认chatId正确传递到Avatar组件
3. 查看DOM中是否有`<style>`标签注入

### 问题3：动画不生效
**可能原因：**
- @keyframes未定义
- CSS语法错误

**解决方法：**
1. 使用预设样式测试
2. 检查自定义CSS语法
3. 确保包含完整的@keyframes定义

## 💡 技术实现

### 数据流
```
ChatSettings (设置页面)
  ↓ 保存到localStorage
  ↓ user_avatar_frame_{chatId}
  ↓ ai_avatar_frame_{chatId}
  ↓
MessageList (消息列表)
  ↓ 传递chatId
  ↓
MessageItem (消息项)
  ↓ 传递chatId
  ↓
Avatar (头像组件)
  ↓ 读取localStorage
  ↓ 注入<style>标签
  ↓ 应用CSS类
  ↓
显示头像框 ✅
```

### 关键文件
- `AvatarFrameSettings.tsx` - 设置组件
- `Avatar.tsx` - 头像组件（支持头像框）
- `MessageItem.tsx` - 传递chatId
- `MessageList.tsx` - 传递chatId
- `VirtualMessageList.tsx` - 传递chatId

## 🎨 预设样式CSS

### 金色光环
```css
border: 2px solid transparent !important;
background: linear-gradient(white, white) padding-box,
            linear-gradient(135deg, #FFD700, #FFA500) border-box !important;
box-shadow: 0 0 15px rgba(255, 215, 0, 0.5) !important;
```

### 彩虹边框
```css
border: 3px solid transparent !important;
background: linear-gradient(white, white) padding-box,
            linear-gradient(135deg, #667eea 0%, #764ba2 25%, #f093fb 50%, #4facfe 75%, #00f2fe 100%) border-box !important;
animation: rainbow-rotate 3s linear infinite !important;
```

### 霓虹发光
```css
border: 2px solid #00ffff !important;
box-shadow: 
  0 0 5px #00ffff,
  0 0 10px #00ffff,
  0 0 20px #00ffff,
  inset 0 0 10px rgba(0, 255, 255, 0.2) !important;
animation: neon-pulse 2s ease-in-out infinite !important;
```

## ✅ 测试清单
- [ ] 能打开头像框设置
- [ ] 能选择预设样式
- [ ] 预览区域显示正确
- [ ] 点击应用后保存成功
- [ ] 返回聊天页面头像框显示
- [ ] 用户头像框正确
- [ ] AI头像框正确
- [ ] 动画效果正常
- [ ] 刷新页面后保持
- [ ] 自定义CSS生效
