# 🚀 部署指南

## ✅ 代码已推送到 GitHub

仓库地址: https://github.com/zhizhi-07/douzhi

提交信息: 
- 67个文件修改
- 新增9858行代码
- 完整实现亲密付、情侣空间、视频通话功能
- 代码质量优化

---

## 📦 方式一：Vercel 部署 (推荐)

### 优点
- ✅ 免费
- ✅ 自动部署
- ✅ 全球CDN
- ✅ HTTPS支持
- ✅ 无需服务器

### 部署步骤

#### 1. 访问 Vercel
https://vercel.com

#### 2. 登录/注册
- 使用GitHub账号登录

#### 3. 导入项目
点击 **"New Project"** 或 **"Add New..."** → **"Project"**

#### 4. 导入 GitHub 仓库
- 搜索或选择: `douzhi`
- 点击 **"Import"**

#### 5. 配置项目
```
Framework Preset: Vite
Build Command: npm run build
Output Directory: dist
Install Command: npm install
```

**环境变量 (可选):**
```
无需配置 (API密钥由用户在应用内设置)
```

#### 6. 部署
点击 **"Deploy"** 按钮

#### 7. 等待部署完成
- 通常需要 1-3 分钟
- 完成后会显示部署URL

#### 8. 访问你的应用
- URL格式: `https://douzhi-xxxx.vercel.app`
- 可以在Vercel设置中绑定自定义域名

---

## 📦 方式二：Netlify 部署

### 部署步骤

#### 1. 访问 Netlify
https://app.netlify.com

#### 2. 登录/注册
- 使用GitHub账号登录

#### 3. 导入项目
点击 **"Add new site"** → **"Import an existing project"**

#### 4. 连接到 Git 提供商
- 选择 **"GitHub"**
- 授权访问

#### 5. 选择仓库
- 搜索: `douzhi`
- 点击选择

#### 6. 配置构建设置
```
Build command: npm run build
Publish directory: dist
```

#### 7. 部署
点击 **"Deploy site"**

#### 8. 访问你的应用
- URL格式: `https://douzhi-xxxx.netlify.app`

---

## 📦 方式三：GitHub Pages 部署

### 前置准备

需要在 `vite.config.ts` 中添加 base 配置：

```typescript
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  base: '/douzhi/' // GitHub仓库名
})
```

### 部署步骤

#### 1. 安装 gh-pages
```bash
npm install -D gh-pages
```

#### 2. 添加部署脚本到 package.json
```json
{
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  }
}
```

#### 3. 部署
```bash
npm run deploy
```

#### 4. 配置 GitHub Pages
- 访问仓库 → Settings → Pages
- Source: 选择 `gh-pages` 分支
- 点击 Save

#### 5. 访问你的应用
https://zhizhi-07.github.io/douzhi/

---

## 🔧 构建测试

在部署前，建议本地测试构建：

```bash
# 构建
npm run build

# 预览构建结果
npm run preview
```

访问: http://localhost:4173

---

## 📊 部署后检查清单

### ✅ 功能测试
- [ ] 聊天功能正常
- [ ] API配置可以保存
- [ ] 角色管理正常
- [ ] 转账功能正常
- [ ] 亲密付功能正常
- [ ] 情侣空间功能正常
- [ ] 视频通话功能正常
- [ ] 钱包功能正常

### ✅ 性能检查
- [ ] 首屏加载速度 < 3秒
- [ ] 页面切换流畅
- [ ] 消息发送响应快
- [ ] 无明显卡顿

### ✅ 兼容性
- [ ] Chrome/Edge 正常
- [ ] Safari 正常
- [ ] Firefox 正常
- [ ] 移动端浏览器正常

---

## 🎯 推荐配置

### Vercel 自动部署

**优点：**
- 每次 push 到 main 分支自动部署
- 预览部署 (PR 自动创建预览环境)
- 零配置

**设置方法：**
1. 在 Vercel 项目设置中
2. Git → Production Branch: `main`
3. 自动部署已启用 ✅

### 自定义域名 (可选)

**在 Vercel:**
1. 项目 → Settings → Domains
2. 添加域名
3. 按提示配置DNS记录

**DNS配置：**
```
Type: CNAME
Name: www (或@)
Value: cname.vercel-dns.com
```

---

## 🐛 常见问题

### Q: 部署后页面空白？
**A:** 检查浏览器Console是否有错误，通常是路由配置问题。确保 `vercel.json` 中有正确的 rewrites 配置。

### Q: API调用失败？
**A:** 检查API配置，确保在应用设置中正确配置了API端点和密钥。

### Q: 构建失败？
**A:** 
1. 检查 `package.json` 中的依赖是否完整
2. 确保 Node.js 版本 >= 16
3. 查看构建日志中的具体错误

### Q: 如何更新部署？
**A:** 
```bash
git add .
git commit -m "update: 更新内容"
git push origin main
```
Vercel 会自动重新部署。

---

## 📝 部署记录

### 初次部署
- 日期: 2025-11-05
- 版本: 1.0.0
- 提交: 6fda2ea
- 状态: ✅ 成功

### 主要功能
- ✨ 聊天系统
- ✨ 钱包系统
- ✨ 亲密付
- ✨ 情侣空间
- ✨ 视频通话
- ✨ 转账功能

### 性能指标
- 代码行数: 20,000+
- 组件数量: 50+
- 页面数量: 20+
- 打包大小: ~300KB (gzipped)

---

## 🎉 部署完成！

恭喜！你的应用已成功部署。

**下一步：**
1. 访问部署URL测试功能
2. 配置自定义域名 (可选)
3. 设置分析工具 (可选)
4. 收集用户反馈
5. 持续迭代优化

**技术支持：**
- GitHub Issues: https://github.com/zhizhi-07/douzhi/issues
- 邮件: support@example.com

---

## 📚 相关文档

- [开发文档](./README.md)
- [代码优化报告](./OPTIMIZATION_PROGRESS.md)
- [重构建议](./REFACTOR_SUGGESTIONS.md)
- [视频通话功能](./VIDEO_CALL_FEATURE.md)
